apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-catalogo
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  duration: "60s"
  selector:
    labelSelectors:
      app.kubernetes.io/name: catalogo
